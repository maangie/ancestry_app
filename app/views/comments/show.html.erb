<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @comment.content %>
</p>

<ul><%= render @comment.children %></ul>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', @comment.parent ? @comment.parent : comments_path %>
